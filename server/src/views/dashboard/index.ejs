<%- include("../partials/head.ejs") %>
    <%- include("../partials/header.ejs") %>
        <div class="layout">
            <%- include("../partials/sidebar.ejs") %>
            <main class="container-secc">
                <section class="secccion-productos">
                    <h2>
                        <%= about %>
                    </h2>
                    <div id="contenedor-productos">
                        <!-- Recibimos nuestro array de productos en la plantilla y lo iteramos en la misma plantilla 
                        Estamos directamente haciendo la accion del innerHTML -->
                        <% productsArray.forEach(producto=> { %>
                            <div class="card-producto" id="card-producto-<%= producto.id %>">
                                <img class="image-producto" src="<%= producto.image_url %>" alt="<%= producto.image_url %>">
                                <h4 class="name-producto"><%= producto.name %></h4>
                                <p class="id-producto">ID: <%= producto.id %></p>
                                <p class="price-producto">$<%= producto.price %></p>
                                <p class="stock-producto"><%= producto.stock %></p>
                                <p class="lineId-producto"><%= producto.line_id %></p>
                                <p class="state-producto <%= producto.state ? 'activo' : 'inactivo' %>">
                                    <%= producto.state ? 'Activo' : 'Inactivo' %>
                                </p>
                                <button 
                                    class="btn-card-producto btn-copy"
                                    data-id="<%= producto.id %>"
                                    title="Copiar ID">
                                    <i data-lucide="copy"></i>
                                </button>
                                <a 
                                    href="/dashboard/modificar?id=<%= producto.id %>"
                                    class="btn-card-producto btn-modify"
                                    title="Modificar producto">
                                    <i data-lucide="pencil"></i>
                                </a>
                                <% if (producto.state) { %>
                                    <button
                                        class="btn-card-producto btn-delete"
                                        data-name="<%= producto.name %>"
                                        data-id="<%= producto.id %>"
                                        title="Desactivar producto">
                                        <i data-lucide="trash-2"></i>
                                    </button>
                                <% } else { %>
                                    <button
                                        class="btn-card-producto btn-activate"
                                        data-name="<%= producto.name %>"
                                        data-id="<%= producto.id %>"
                                        title="Activar producto">
                                        <i data-lucide="power"></i>
                                    </button>
                                <% } %>
                            </div>
                        <% }) %>
                    </div>
                </section>
            </main>
        </div>

        <script src="/js/dashboard/index.js"></script>

        <%- include("../partials/footer.ejs") %>